# Browser-accessible (bundled into the SPA — safe public keys only)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Runtime-only, used by api/extract.ts (never bundled into the browser)
GEMINI_API_KEY=
